# Absolute minimal config with Cog's built-in torch support
build:
  gpu: true
  cuda: "12.6"
  torch_version: "2.6.0"
  python_version: "3.11"
  python_packages:
    - "Pillow"
predict: "predict.py:Predictor"
